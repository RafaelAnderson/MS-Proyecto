server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/RafaelAnderson/Microservice-Config
  application:
    name: config-server